enum colours { white, orange, green, red, blue, yellow };

class Cube {
    // properties
    colours faces[6][4];

    /**
     * solves the cube and returns the algorithm
    */
    int solve(char [20]) {
        return false;
    }

        
};